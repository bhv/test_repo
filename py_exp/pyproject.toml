[project]
name = "py-exp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.3",
]

[dependency-groups]
experimental = [
    "jupyter>=1.1.1",
]
